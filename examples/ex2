
(define k::bool)
(define kp::bool)
(define r1::bool)
(define r2::bool)
(assert (neq (pr (and k (and r1 r2))) (pr (and kp (and r1 r2)))))
(assert (= (pr (and r1 r2)) (/ 1 4)))
(assert (= (pr r1) (/ 1 2)))
(assert (= (pr r2) (/ 1 2)))
(check)
(show-model)
